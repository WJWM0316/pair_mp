<view class="step3">
  <view class="h1">完善职业信息</view>
  <view class="progress">
    <view class="item active-item"></view>
    <view class="item active-item"></view>
    <view class="item active-item"></view>
  </view>
  <view class="ul">
    <view class="li">
      <view class="key">职业</view>
      <input
        placeholder-class="holderStyle"
        placeholder="请选择"
        value="{{formData.resident_str}}"
        class="value {{formData.resident_str ? 'bold' : ''}}"
        disabled/>
    </view>
    <view class="li">
      <view class="key">所在公司</view>
      <input
        placeholder-class="holderStyle"
        placeholder="请选择"
        value="{{formData.company_name}}"
        class="value {{formData.company_name ? 'bold' : ''}}"
        disabled/>
    </view>
    <view class="li">
      <view class="key">担任职位</view>
      <input
        placeholder-class="holderStyle"
        placeholder="请选择"
        value="{{formData.position_name}}"
        class="value {{formData.position_name ? 'bold' : ''}}"
        disabled/>
    </view>
  </view>
  <view class="tips-box">
    <block wx:if="{{ status === 1}}">
      <view class="info"><i class="icon"></i>职业信息未认证，将降低主页展示率</view>
      <view class="strong">前往认证</view>
    </block>
    <block wx:elif="{{ status === 2}}">
      <view class="info"> 职业信息认证中</view>
      <view class="strong">更改认证信息</view>
    </block>
    <block wx:else>
      <view class="info"><i class="icon"></i>职业信息认证成功</view>
    </block>
  </view>
  <i-button type="success" class="btn-next btn-next-active" long="{{ true }}">完成</i-button>
</view>
