<view class="emojiWrap" catchtap = "false">
  <view class="list">
    <view class="emoji" wx:for="{{list}}" bindtap = "selected" data-index="{{index}}" wx:key="index">
      <i-image my-class="emojiImg" src="{{cdnPath}}emoji/{{item}}.png"></i-image>
    </view>
  </view>
</view>