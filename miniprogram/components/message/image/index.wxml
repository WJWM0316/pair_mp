<i-image 
  class="i-image"
  hasLodingImg="{{false}}"
  my-class="msg-image"
  styleString="{{styleString}}" 
  showMenu="{{false}}" 
  src="{{message.imageUri}}"
  catchtap = "previewImage"
  bindloadSuccess="loadSuccess"></i-image>
