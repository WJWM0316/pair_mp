<button class="my-class" open-type="contact" send-message-img="{{cardData.img}}" send-message-title="{{cardData.title}}"  show-message-card="true">
  <block wx:if="{{title}}">{{title}}</block>
  <block wx:else><slot name="content"></slot></block>
</button>
